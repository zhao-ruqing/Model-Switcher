# TODO List - AI 配置切换器拓展计划

为了提升项目的可拓展性，支持更多大模型工具和 Provider，计划进行以下改进：

## 核心架构改进
- [ ] **动态字段渲染**：不再在 `index.html` 中硬编码表单字段，改为根据工具类型（Claude/OpenCode/NewTool）从配置文件中读取字段定义进行动态渲染。
- [ ] **插件式切换逻辑**：将 `server.js` 中的 `switchClaude` 和 `switchOpencode` 抽离为独立的处理器模块，方便通过增加新文件来支持新工具。
- [ ] **通用配置模型**：统一内部 `config.json` 的存储格式，使用更通用的 metadata 结构来适配不同工具的特殊需求。

## 功能增强
- [ ] **多端同步**：支持导出/导入配置，或通过 Gist 同步配置数据。
- [ ] **状态检测**：在界面上增加“检查连通性”按钮，实时测试 API Key 是否可用。
- [ ] **多 Provider 支持**：预设更多主流模型（如 Gemini, Mistral, Groq）的参数模板。

## 体验优化
- [ ] **自动更新**：增加检测本地 `launcher` 版本的逻辑。
- [ ] **系统托盘**：使用 Electron 或轻量级工具将启动器常驻系统托盘，方便快速切换。
- [ ] **暗黑模式**：适配现代 UI 审美，增加暗黑/明亮主题切换。

## 文档与规范
- [ ] **API 文档**：为 `server.js` 提供的接口编写标准文档。
- [ ] **贡献指南**：编写如何接入一个新工具的 Step-by-step 指南。
