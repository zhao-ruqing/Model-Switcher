# 2026-05-29 13:30 按需启动与 API 测试修复

## 本次开发进度

### launcher.js 自守护化

- 新增 TTY 检测逻辑：终端运行时自动 spawn 后台子进程并退出，任务计划/非 TTY 环境直接作为守护进程运行
- 子进程等待父进程探测端口就绪后才退出，避免竞态条件
- 新增 `EADDRINUSE` 错误处理，重复启动时优雅退出而非崩溃

### 开机自启脚本

- 新增 scripts/setup-autostart.ps1：在 Startup 文件夹创建快捷方式，无需管理员权限
- 登录时自动运行 launcher.js（隐藏窗口），实现真正的按需启动

### API 测试逻辑修复

- 修复 Claude 类型配置使用第三方 Anthropic 兼容 API 时测试失败的问题
- server.js 中根据 Base URL 自动选择测试器：URL 含 `/anthropic` 时使用 Anthropic 测试器（POST /v1/messages），否则按 providerType 选择
- Deepseek、Mimo 等第三方 API 测试现已正常通过

### README 文档更新

- 快速开始流程调整为：安装依赖 → 设置自启（一次）→ 直接访问浏览器
- 目录结构加入 setup-autostart.ps1

## 修改文件清单

- 修改 launcher.js：新增自守护逻辑（+49 行）
- 新增 scripts/setup-autostart.ps1：Startup 文件夹快捷方式脚本
- 修改 server.js：API 测试器自动选择逻辑（+5 行，-2 行）
- 修改 README.md：更新快速开始和目录结构
